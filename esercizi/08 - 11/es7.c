#include <stdio.h>
#include <string.h>

#define N 30

int main()
{
    char s1[N], s2[N];
    int l1, l2, i, j;

    printf("ciao bello inserisci la stringa 1");
    scanf("%s", s1);
    printf("ciao bello inserisci la stringa 2");
    scanf("%s", s2);

    l1 = strlen(s1);
    l2 = strlen(s2);

    for(i = 0; i < l1; i++){
        if(s1[i] = s2[0]){
            
        }
    }
}